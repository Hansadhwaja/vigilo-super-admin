export type TenantGrowthData = {
  month: string
  tenants: number
}

export type RevenueGrowthData = {
  month: string
  revenue: number
}

export type PlanDistributionData = {
  plan: string
  subscribers: number
  fill: string
}