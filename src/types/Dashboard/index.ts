export type TenantGrowthData = {
  month: string
  count: number
}

export type RevenueGrowthData = {
  month: string
  total: number
}

export type PlanDistributionData = {
  planName: string
  count: number
  interval: string
  planId: string
}
